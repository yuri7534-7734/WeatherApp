
  # Weather app with OpenWeather

  This is a code bundle for Weather app with OpenWeather. The original project is available at https://www.figma.com/design/BkjikW03v29fCG1tagWfhd/Weather-app-with-OpenWeather.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  